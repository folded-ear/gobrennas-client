const TemporalActions = {
    EVERY_15_SECONDS: "temporal/every-15-seconds",
    EVERY_MINUTE: "temporal/every-minute",
    EVERY_HOUR: "temporal/every-hour",
    EVERY_MORNING: "temporal/every-morning",
    EVERY_AFTERNOON: "temporal/every-afternoon",
    EVERY_EVENING: "temporal/every-evening",
    EVERY_NIGHT: "temporal/every-night",
    EVERY_DAY: "temporal/every-day",
}

export default TemporalActions